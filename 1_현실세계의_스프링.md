# 1. 현실 세계의 스프링

## 1.1 마이크로서비스 아키텍처로 진화

- __스프링 프레임워크란 ?__
  
  애플리케이션 프레임워크 : 애플리케이션을 개발하는 기본 구조를 제공하는 공통 소프트웨어 기능의 집합  
  많은 애플리케이션의 요구사항은 유사하다  
  - 로깅 오류, 경고(warning), 정보(info) 메시지는 모든 앱에서 발생한다.
  - 대부분의 애플리케이션은 트랜잭션을 사용하여 데이터 변경을 처리한다.
  - 대부분 공통으로 발생하는 동일한 취약점에 보호 메커니즘을 사용한다.
  - 대부분 유사한 방법으로 상호 통신한다.
  - 캐싱 또는 데이터 압축처럼 성능을 향상하는 데 유사한 메커니즘을 사용한다.  
  
  다양한 애플리케이션에서 비즈니스로직을 제외한, 비업무용으로 구현한 기능들을 재사용할 수 있다. 재사용함으로써 얻는 이득은 아래와 같다.
  - 자체 개발하지 않고 재사용함으로써 많은 시간과 비용을 절약할 수 있다.
  - 이미 많은 앱에서 사용하는 기존 구현체는 다른 사람이 이미 테스트했기 때문에 버그를 불러올 가능성이 낮다.
  - 동일한 기능을 이해하는 많은 개발자에게 조언을 구할 수 있기 때문에 커뮤니티 혜택을 볼 수 있다. 직접 코드를 구현했다면 그것을 아는 사람은 소수에 불과하다.
  
## 1.2 스프링 생태계

  - __스프링 코어 : 스프링의 기본__  
    기본 기능을 포함하는 스프링의 기반 부분 중 하나. 
    - __스프링 컨텍스트__ : 스프링이 앱의 인스턴스를 관리할 수 있게 하는 스프링 프레임워크의 기본 기능.
    - __스프링 애스팩트__ : 스프링이 앱에서 정의한 메서드를 가로채고 조작할 수 있게 하는 기능.
    - __스프링 표현 언어(spEL)__ : 특정 언어를 사용하여 스프링 구성 내용을 작성
  - __스프링 모델-뷰-컨트롤러(MVC)__
    HTTP 요청을 처리하는 웹 애플리케이션을 개발할 수 있게 하는 스프링 프레임워크 일부분.
  - __스프링 데이터 액세스(Spring Data Access)__
    SQL 데이터베이스에 연결하여 앱 영속성 계층을 구현하는 데 사용할 수 있는 기본 도구를 제공
  - __스프링 테스팅__
    스프링 애플리케이션 테스트를 작성하는 데 필요한 도구를 담고 있다.

### 1.2.1 스프링 코어의 이해: 스프링 기초

  - __IOC(제어의 역전)__
    - 스프링은 제어의 역전 원칙을 기반으로 작동한다. 앱이 실행을 제어하는 대신 다른 소프트웨어 부분, __스프링 프레임워크에 제어 권한__ 을 넘긴다. __개발자는 구성(configuration)을 이용하여 앱 로직을 정의__ 하도록 작성된 코드 관리 방법을 프레임워크에 지시한다.  
    - __제어 역전에서 '역전'은 앱이 자체 코드로 실행을 제어하거나 의존성을 사용하지 못하는 대신 프레임워크(의존성)가 앱과 앱의 코드를 제어한다는 의미이다.__
    - 이 맥락에서 __제어(controls)__ 라는 용어는 __'인스턴스 생성' 또는 '메서드 호출'__ 과 같은 작업을 나타낸다. 예를 들어 스프링은 특정 메서드를 가로채서 메서드 실행 중에 나타날 수 있는 오류를 기록할 수 있다.
    - __IOC 컨테이너__ 는 __애플리케이션의 스프링 구성 요소와 애플리케이션의 구성 요소를 프레임워크에 결합__ 한다. __스프링 컨텍스트__ 라고 하는 IoC 컨테이너를 사용하여 특정 객체를 스프링에 전달해서 프레임워크가 구성한 방식으로 객체를 사용할 수 있게 한다.
    - __메서드 애스펙팅(aspecting the method)__ 는 __인스턴스의 동작인 메서드를 가로채는 것__ 으로, 프레임워크가 앱과 상호 작용하는 일반적인 방법 중 하나.

### 1.2.2 스프링 데이터 액세스 기능을 사용한 앱 영속성 구현

  - 스프링에서는 많은 경우에 데이터 영속성을 관리하는 데 데이터 액세스(Data Access) 모듈을 사용한다. 스프링 데이터 액세스에는 JDBC 사용, 하이버네이트(Hibernate)와 같은 객체 관계형 매핑(Object-Relational Mapping, ORM) 프레임워크와 통합, 트랜잭션 관리가 포함된다.
  
### 1.2.3 웹 앱 개발을 위한 스프링 MVC 기능

  - 스프링 MVC를 

### 1.2.4 스프링 테스트 기능

### 1.2.5 스프링 생태계의 프로젝트




  __Spring Data Access 모듈__  

  __스프링 부트__ : 구성보다 관례
  모든 구성을 사용자가 설정하는 대신 필요한 부분만 변경